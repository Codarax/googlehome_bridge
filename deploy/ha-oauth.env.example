# Copy to /etc/ha-oauth.env and set secure values (DO NOT commit real secrets)
# Example environment variables for the HA OAuth bridge
CLIENT_ID=Yew6FCGaG5ALIfNaZzWBZXBBLkaOnP8e
CLIENT_SECRET=HtHL5dDmYqtMpHWduEdHjkA0nOVJByNg
HA_URL=https://homeassistant.codarax.nl
HA_TOKEN=your_long_lived_access_token_here   # e.g. a Home Assistant long-lived access token
ADMIN_API_KEY=optional_admin_key_here        # strong random string; used to protect admin endpoints
DEVICES_FILE=/opt/ha-oauth/devices.json
TOKENS_FILE=/var/lib/ha-oauth/tokens.json
USE_FILE_STORAGE=true
EXPOSE_TEMPERATURE=true
EXPOSE_HUMIDITY=true
EXPOSE_GENERIC=false
EXPOSE_POWER=false
MAX_DEVICES=50
# Security: after copying to /etc/ha-oauth.env, set strict permissions and restrict access:
# sudo mv deploy/ha-oauth.env.example /etc/ha-oauth.env
# sudo chown root:root /etc/ha-oauth.env
# sudo chmod 600 /etc/ha-oauth.env
# Do not store tokens in source control or world-readable locations.
